package project0;

public class Maths {
	/**
	 * You are to find the factorial of a and the factorial of b, and add them together.
	 * @param a
	 * @param b
	 * @return a! + b!
	 */
	public static int sumFactorials(int a, int b) {
		//return Utilities.factorial(a) + Utilities.factorial(b);
        return 0;
	}
	
	/**
	 * You are to find the nth Fibonacci number (n=0->0, n=1->1) 
	 * @param n
	 * @return The nth Fibonacci number
	 */
	public static int fibonacci(int n) {
		//if (n < 2) {
		//	return n;
		//} else {
		//	return fibonacci(n - 1) + fibonacci(n - 2);
		//}
        return 0;
	}
}
